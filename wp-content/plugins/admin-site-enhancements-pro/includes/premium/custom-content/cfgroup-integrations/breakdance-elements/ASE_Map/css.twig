%%SELECTOR%% {
  width: {{ design.size.width.style }};
  height: auto;
  {{ macros.spacing_margin_y(design.spacing) }}
}

%%SELECTOR%% .asenha-cfgroup-map-osm,
%%SELECTOR%% iframe {
  width: 100%;
  height: {{ design.size.height.style }};
  border: 0;
}
